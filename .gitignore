node_modules/
venv/
dist/
*.log
.env
__pycache__/
*.pyc
.claude/

# วิดีโอ/ข้อมูลดิบสำหรับเทรนโมเดล (~4.7GB) ใหญ่เกินจะเก็บใน git — สำรองแยกต่างหาก
# (เช่น external drive/cloud) แทน ไฟล์เหล่านี้ยังติดไปกับการก็อปทั้งโฟลเดอร์ไปเครื่องอื่นอยู่ดี
ai-engine/dataset/
