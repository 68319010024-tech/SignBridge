# 1. ย้ายเข้าไปที่โฟลเดอร์ AI Engine
cd ai-engine

# 2. ติดตั้ง Dependencies สำหรับพัฒนาและเทรน
pip install -r requirements.txt

# 3. สกัด พิกัด Landmarks จากวิดีโอ MP4 ใน raw_videos/
python src/01_extract_landmarks.py

# 4. เทรนโมเดล TensorFlow แล้วบันทึกเป็น gesture_model.h5
python src/02_train_model.py

# 5. ทดสอบการจำแนกท่าทางผ่านกล้อง Webcam บนเครื่อง Local
python src/03_realtime_inference.py